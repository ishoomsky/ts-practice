const arr = [1,2,3];
arr.includes(2); //es2016 feature

console.log('123'); // we should add relevant value (e.g. "DOM") to the 'lib' list

Math.random(); // we should add relevant value (e.g. "ES2016") to the 'lib' list